package com.pathly.domain.model

/**
 * 場所（places）の由来。自動回収（参照ゼロの掃除）の対象判定に使う。
 *
 * - [DETECTED]: 記録中の自動検出が作った場所。参照が無くなれば自動回収してよい（誤検知の後始末）。
 * - [USER]: ユーザーが明示的に作った場所（場所登録・手動立ち寄り追加・キーワード検索）。
 *   訪問も行きたいも無くても**自動では消さない**（意図的に作ったものを勝手に消さないため）。
 */
enum class PlaceSource {
  DETECTED,
  USER,
}
